# 해나루 종합설비 홈페이지 — 기능 정의서 (Functional Spec)

> 이 문서는 **Claude Code가 읽고 그대로 구현하는 명세서**다. 사람용 기획서가 아니라 빌드 스펙이므로, 모호한 표현은 피하고 결정된 값으로 기술한다.
> 프로젝트 규칙(폴더 구조 · 네이밍 · 경로 · 환경변수 · 가드레일)은 **별도 파일 `CLAUDE.md`** 에 정의되어 있으며, 빌드 전 반드시 함께 읽는다.

---

## 0. 한 줄 요약

설비 1인 회사(해나루 종합설비)의 **홍보/시공사례 사이트**. 공개 페이지(누구나) + 관리자 영역(`/admin`, 사장·직원 2인). 관리자가 현장 사진과 작업 정보를 올리면 공개 시공사례에 자동 반영되고, 사진의 촬영일·위치는 EXIF에서 자동 추출(없으면 수동 입력). **방문자 문의는 카카오톡 채널 1:1 채팅 버튼**으로 받는다(웹 폼·DB·이메일 발송 없음). 모바일에서는 PWA로 앱처럼 설치. **전 구간 무료 티어로 운영(도메인만 유료 구매)**.

---

## 1. 프로젝트 개요

### 1.1 목적
- 회사 신뢰도 전달(연혁·실적·시공사례)
- 카카오톡 채널을 통한 **문의 유입**
- 사장이 직접 시공사례를 쉽게 누적 관리

### 1.2 타겟 사용자
| 그룹 | 설명 | 접근 영역 |
|---|---|---|
| 잠재 고객 | 설비 시공 의뢰 검토하는 일반 방문자 | 공개 페이지 |
| 운영자 | 사장 + 직원(최대 2명) | `/admin` |

### 1.3 범위
- **포함**: 공개 홍보 페이지, 시공사례 CRUD, 사진 업로드 자동정리(EXIF), 카카오톡 채널 문의 버튼, 관리자 대시보드, PWA, SEO, 오시는 길
- **제외**: 웹 문의 폼/문의 DB/이메일 발송, 온라인 결제·예약, 회원가입, 다국어(국문만), 인-앱 실시간 접속자 카운터(외부 분석 도구 링크로 대체)

### 1.4 비목표(Non-goals) — 만들지 말 것
- 웹 문의 폼 및 문의 데이터 저장(DB)·이메일 발송 (→ 문의는 카카오톡 채널로 일원화)
- 공개 회원 시스템 / 소셜 로그인
- 자체 실시간 트래픽 집계 대시보드
- 결제·견적 자동계산 기능
- 관리자 외 사용자 권한 등급(역할은 단일: admin)

---

## 2. 기술 스택 (확정 — 길 2: All-Cloudflare)

| 레이어 | 기술 | 비고 |
|---|---|---|
| 프레임워크 | **Next.js 15 (App Router) + TypeScript** | 정적+동적 혼합, RSC 기본 |
| 배포 어댑터 | **@opennextjs/cloudflare** | Next.js를 Cloudflare에서 구동 |
| 호스팅 | **Cloudflare Pages/Workers** | 무료·상업용 허용·대역폭 무제한. GitHub 레포 연결 자동배포 |
| DB | **Cloudflare D1 (SQLite)** + **Drizzle ORM** | 멈춤 없음, 타입세이프 |
| 파일 저장 | **Cloudflare R2** | 현장 사진 원본/리사이즈/썸네일 |
| 관리자 인증 | **Cloudflare Access (Zero Trust)** | 이메일 허용목록 2개. 앱에 비번 저장 안 함 |
| **문의** | **카카오톡 채널 1:1 채팅 버튼 (Kakao JS SDK)** | 폼/DB/메일 없음. 문의는 카톡 채널 관리자센터로 |
| 스타일 | **Tailwind CSS v4** | 디자인 토큰은 CSS 변수로(§12) |
| 폼 검증 | **zod** | 관리자 입력 검증용(공개 폼은 없음) |
| EXIF 추출 | **exifr** (클라이언트) | 날짜·GPS |
| HEIC 변환 | **heic2any** (클라이언트) | 아이폰 사진 → JPEG |
| 이미지 리사이즈 | 브라우저 **canvas** (필요 시 `pica`) | 업로드 전 웹용/썸네일 생성 |
| 지도 | **Kakao Maps JS SDK** | 오시는 길 |
| 분석 | **Cloudflare Web Analytics** | 무료, 관리자에서 외부 링크로 확인 |
| PWA | **Serwist** (App Router 호환 SW) + manifest | 홈 화면 추가 |

> 버전은 `package.json`에 고정(pin)한다. 임의 메이저 업그레이드 금지(→ `CLAUDE.md` 가드레일).

---

## 3. 사이트 구조 (IA)

### 3.1 사이트맵
```
/                      메인(홈)
/about                 회사소개
/services              사업영역(배관·보일러·난방 …)
/works                 시공사례 목록 (+ 카테고리 필터)
/works/[id]            시공사례 상세
/location              오시는 길 (카카오맵)
/contact               연락처 (전화 · 카카오톡 채널 채팅 버튼 · 영업시간)  ※웹 폼 아님
/privacy               개인정보처리방침 (분석·카카오 채널 이용 고지)

/admin                 관리자 대시보드            (Cloudflare Access 보호)
/admin/works           시공사례 관리(목록·검색)   (보호)
/admin/works/new       시공사례 등록(사진 업로드) (보호)
/admin/works/[id]/edit 시공사례 수정             (보호)
```

> 전 페이지 공통: **카카오톡 채널 채팅 플로팅 버튼**(우하단 고정). `/contact`에는 동일 버튼을 본문에도 배치.

### 3.2 페이지 목록표
| 경로 | 유형 | 데이터 출처 | 우선순위 |
|---|---|---|---|
| `/` | 공개 | D1(대표 시공사례·카운트) + config | P0 |
| `/about` | 공개 | content 파일 | P1 |
| `/services` | 공개 | content 파일(카테고리) | P1 |
| `/works`, `/works/[id]` | 공개 | D1 + R2 | P0 |
| `/location` | 공개 | config + Kakao SDK | P1 |
| `/contact` | 공개 | config + Kakao 채널 버튼 | P0 |
| `/privacy` | 공개 | content 파일 | P1 |
| `/admin/*` | 보호 | D1 + R2 | P0 |

---

## 4. 페이지별 기능 명세 (섹션 단위)

> Claude Code는 **페이지 통째가 아니라 섹션 컴포넌트 단위**로 구현한다. 각 섹션은 독립 컴포넌트로 분리.

### 4.1 메인 `/`
1. **Hero** — 회사명/슬로건/대표 이미지, 핵심 CTA(전화 `tel:` + 카카오톡 채널 채팅)
2. **회사 요약 스트립** — `창업연도`, **누적 시공 건수**(§8 로직), 취급 분야 요약. 숫자 카운트업 애니메이션(선택).
3. **대표 시공사례** — D1에서 최신/대표 표시 N건(기본 6건) 카드 그리드 → `/works/[id]`
4. **사업영역 요약** — 배관/보일러/난방 등 카테고리 아이콘 카드 → `/services`
5. **오시는 길 미리보기** — 지도 썸네일 + 주소 → `/location`
6. **CTA 배너** — 전화(`tel:`) + **카카오톡 채널 채팅 버튼**
7. **푸터**(공통, §4.10)

> ※ 메인 상세 레이아웃은 사용자가 추후 초안 제공 예정. 본 구조를 기본 골격으로 두고, 초안 수령 시 섹션 순서/카피만 교체 가능하도록 **섹션 컴포넌트를 느슨하게 결합**한다.

### 4.2 회사소개 `/about`
- 인사말, 연혁(타임라인), 비전/강점. 텍스트는 `content` 파일에서 주입(§11). 코드에 본문 하드코딩 금지.

### 4.3 사업영역 `/services`
- 카테고리 리스트(배관/보일러/난방 …)를 **데이터 파일**(`src/content/services.ts`)에서 렌더. 항목 추가가 파일 한 줄 수정으로 끝나야 함(향후 사장과 확장 예정).
- 각 카테고리: 제목, 설명, 대표 아이콘/이미지.

### 4.4 시공사례 목록 `/works`
- D1 `works`에서 공개(`is_published=1`) 항목을 최신순 페이지네이션(기본 12개/페이지).
- **카테고리 필터**(쿼리스트링 `?category=`), 카드: 대표사진 + 제목 + 구·동 단위 위치 + 날짜.

### 4.5 시공사례 상세 `/works/[id]`
- 제목, 날짜, **구·동 단위 위치**(상세주소·좌표 비공개), 작업 설명, 사진 갤러리(대표 우선 + 라이트박스), 카테고리 태그.
- 하단에 카카오톡 채널 채팅 CTA("이런 작업 문의하기").
- SEO: 동적 메타/OG(대표사진).

### 4.6 오시는 길 `/location`
- 카카오맵 SDK로 사업장 위치 핀 + 주소 텍스트 + 전화. 주소/좌표는 `site-config`에서 주입.

### 4.7 연락처 `/contact` (웹 폼 아님)
- **전화번호(`tel:` 링크)**, **카카오톡 채널 1:1 채팅 버튼**, 영업시간, 사업장 주소(+지도 링크).
- 카카오 채널 버튼은 Kakao JS SDK `createChatButton()`/`chat()` 사용. 채널 `channelPublicId`는 환경변수에서 주입.
- **개인정보 입력 필드 없음** — 문의는 전부 카카오톡 채널로 유도. 별도 동의·DB 저장 불필요.

### 4.8 개인정보처리방침 `/privacy`
- 본문은 `content` 파일. 다음을 고지: Cloudflare Web Analytics(쿠키리스) 사용, 카카오톡 채널 상담 시 카카오 정책 적용, 사이트 자체는 방문자 개인정보를 수집·저장하지 않음(폼 없음). 처리위탁(Cloudflare/Kakao) 명시.

### 4.9 관리자 `/admin/*` (Cloudflare Access 보호)
- **대시보드 `/admin`**: 시공사례 총계/최근 등록, 외부 분석(Cloudflare Web Analytics) **링크**, 빠른 등록 버튼.
- **시공사례 관리 `/admin/works`**: 목록·검색·공개여부 토글·삭제(soft delete 권장).
- **등록/수정 `/admin/works/new`·`/[id]/edit`**:
  - 사진 다중 업로드 → 업로드 파이프라인(§6) 자동 실행
  - EXIF에서 날짜·위치 자동 채움 → **비어 있으면 수동 입력 필드 노출(폴백 필수)**
  - 제목(수동), 작업 설명, 카테고리 태그, 대표사진 지정, 공개여부
- (문의 관리 화면 없음 — 문의는 카카오톡 채널 관리자센터에서 처리)

### 4.10 공통
- **헤더**: 로고, 메뉴, 모바일 햄버거, 전화 CTA.
- **카카오톡 채널 플로팅 버튼**: 전 페이지 우하단 고정.
- **푸터**: 상호·대표·사업장 주소·전화·사업자번호(자리), 개인정보처리방침 링크, 카피라이트.
- 전 페이지 반응형(모바일 우선). 메뉴는 모바일에서 앱처럼 동작.

---

## 5. 데이터 모델 (D1 / Drizzle)

> 스키마는 `src/db/schema.ts`. 마이그레이션은 `drizzle-kit` + `wrangler d1 migrations`(§CLAUDE.md). **문의(inquiries) 테이블 없음.**

### `works` (시공사례)
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | text(PK, uuid) | |
| title | text | 수동 입력 |
| description | text | 작업 설명 |
| category | text | services 카테고리 키 |
| worked_at | text(ISO date) | EXIF 자동 → 없으면 수동 |
| location_region | text | 공개용 구·동 (예: "달성군") |
| location_detail | text(nullable) | 내부용 상세, **공개 안 함** |
| lat / lng | real(nullable) | 내부용, 공개 안 함 |
| cover_photo_id | text(nullable, FK) | 대표사진 |
| is_published | integer(0/1) | 공개 여부 |
| created_at / updated_at | text(ISO) | |
| deleted_at | text(nullable) | soft delete |

### `work_photos`
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | text(PK, uuid) | |
| work_id | text(FK → works.id) | |
| r2_key_full | text | 웹용(최대 1600px) R2 키 |
| r2_key_thumb | text | 썸네일(최대 400px) R2 키 |
| width / height | integer | |
| sort_order | integer | 정렬 |
| created_at | text(ISO) | |

### `site_config` (운영값)
- `established_year`, `baseline_project_count`(과거 실적 보정치), 사업장 주소/좌표/전화/사업자번호, 카카오 채널 ID, 영업시간.
- **권장: D1이 아닌 `src/content/site-config.ts` 정적 파일**로 두어 사장이 숫자만 쉽게 수정. (자주 안 바뀌므로)

---

## 6. 사진 업로드 파이프라인 (자동 정리)

업로드는 **관리자 영역에서만** 발생. 클라이언트단에서 전처리 후 R2 적재.

```
[관리자가 사진 선택]
  → (아이폰 HEIC면) heic2any로 JPEG 변환
  → exifr로 EXIF 읽기: 촬영일(DateTimeOriginal), GPS(lat/lng)
        ├ 날짜 있음 → worked_at 자동 채움 / 없음 → 수동 입력 필드 노출
        └ GPS 있음 → 좌표 저장 + 역지오코딩으로 구·동 추정 / 없음 → 위치 수동 입력
  → canvas 리사이즈: 웹용(최대 1600px), 썸네일(최대 400px)  ※원본 대용량 업로드 금지
  → R2 업로드(키 규칙: works/{workId}/{photoId}_full.jpg, _thumb.jpg)
  → D1 work_photos 행 생성, 첫 장을 기본 대표(cover)로
```

- **역지오코딩**: 카카오 로컬 API(좌표→행정동) 사용, 결과는 **구·동까지만** 저장/공개.
- 위치·날짜가 모두 비는 경우에도 등록이 막히지 않도록 수동 입력으로 완료 가능해야 함.
- R2 공개 서빙: 전용 라우트 `/img/[...key]`(Worker) 또는 R2 커스텀 도메인. **원본은 비공개, 웹용/썸네일만 공개.**

---

## 7. 문의 = 카카오톡 채널 1:1 채팅

- 웹 폼·서버 처리·DB 저장·이메일 발송 **없음.**
- Kakao JS SDK 로드 후 `Kakao.init(JS_KEY)` → `Kakao.Channel.createChatButton({ container, channelPublicId })` 또는 클릭 핸들러에서 `Kakao.Channel.chat({ channelPublicId })` 호출.
- 동작: 방문자 탭 → 카카오톡 1:1 채팅방 진입(PC/모바일, 친구추가 불필요) → **사장이 카카오톡 채널 관리자센터/앱에서 응답**.
- 사전 준비(운영자): 카카오톡 채널 개설 → 카카오 디벨로퍼스 앱 생성·채널 연결 → JS 키 + `channelPublicId` 발급(환경변수로 주입).
- 사이트에서 채팅 내용을 끌어오는 기능은 구현하지 않음(해당 API 없음).

---

## 8. 누적 시공 카운터 로직

```
표시값 = baseline_project_count(과거 실적 보정치) + COUNT(works WHERE is_published=1 AND deleted_at IS NULL)
```
- `baseline_project_count`, `established_year`는 `site-config.ts`의 **예시값(placeholder)**로 시작 → 사장과 협의 후 숫자만 교체.
- 메인 §4.1-2와 연동.

---

## 9. 인증 · 보안

- `/admin/*`는 **Cloudflare Access**로 보호. 허용 이메일 2개를 Access 정책(allowlist)에 등록. 공개 가입 없음.
- 앱은 Access가 주입하는 인증 헤더/JWT(`Cf-Access-Jwt-Assertion`)로 로그인 사용자 식별만 한다. **비밀번호를 앱에서 저장·검증하지 않는다.**
- 미들웨어에서 `/admin` 요청에 대해 Access 인증 여부 확인(이중 방어).
- 모든 시크릿(Kakao 등)은 환경변수. `.dev.vars`/Wrangler secret로 관리, 절대 커밋 금지(→ `CLAUDE.md`).
- 개인정보: 사이트가 방문자 개인정보를 직접 수집·저장하지 않음(폼 없음). 시공사례의 상세주소/좌표는 내부 보관·비공개.

---

## 10. 비기능 요구사항

- **SEO**: 페이지별 메타/OG 태그, `sitemap.xml`, `robots.txt`, 시멘틱 마크업, 시공사례 동적 메타.
- **성능**: 이미지 지연로딩, 썸네일 우선, RSC로 초기 페이로드 최소화.
- **접근성**: 대체텍스트(사진 alt = 제목/카테고리 기반), 키보드 내비, 명도 대비.
- **반응형**: 모바일 우선, 주요 브레이크포인트 정의.
- **브라우저**: 최신 크롬/사파리/엣지 + 모바일 사파리/크롬.

---

## 11. PWA

- `manifest.webmanifest`(이름·아이콘·테마컬러·`display: standalone`).
- Serwist 서비스워커로 정적 자산 캐시(오프라인 시 기본 셸). 관리자 영역은 캐시 정책 보수적으로.
- iOS "홈 화면에 추가" 메타 포함.

---

## 12. 디자인 토큰 (TBD — Figma 확정 후 채움)

- 컬러·타이포·간격·radius를 **CSS 변수 한 곳**(`src/styles/tokens.css`)에 정의. 컴포넌트는 변수만 참조(하드코딩 색상 금지).
- 현재 값은 **placeholder**(중립 팔레트)로 두고, Figma 확정 시 토큰 파일만 교체하면 전체 반영되도록 구성.
- 로고: `public/brand/logo.*` 자리 확보. 실제 파일 수령 후 교체.
- 참조: Figma 링크(추후 삽입).

---

## 13. 분석

- Cloudflare Web Analytics 스니펫 삽입(쿠키리스). 관리자 대시보드에는 **대시보드 외부 링크**만 노출(일/주 요약). 자체 실시간 집계는 만들지 않음.

---

## 14. 빌드 마일스톤 (이 순서대로 진행)

> 한 번에 전체를 시키지 말고 단계별 태스크로 실행. 각 단계 종료 시 빌드/배포 확인.

1. **M1 — 스캐폴딩 & 인프라**: Next.js+TS+Tailwind 생성, @opennextjs/cloudflare 연결, Cloudflare 프로젝트(GitHub 레포 연결)/도메인/D1/R2 바인딩, 폴더 구조(§CLAUDE.md) 확립, 빈 페이지 라우팅, manifest.
2. **M2 — 디자인 시스템 & 공통**: 토큰(placeholder), 헤더/푸터, 카카오 채널 플로팅 버튼, 공통 UI 컴포넌트, 반응형 레이아웃.
3. **M3 — 공개 콘텐츠 페이지**: 메인 골격, about/services/privacy(content 주입), location·contact(카카오맵 + 채널 버튼 + 전화).
4. **M4 — 시공사례(읽기)**: D1 스키마/마이그레이션, `/works` 목록·필터·상세, R2 서빙 라우트.
5. **M5 — 관리자 & 업로드**: Cloudflare Access 보호, `/admin` CRUD, 사진 파이프라인(HEIC/EXIF/리사이즈/R2), 누적 카운터.
6. **M6 — 마감**: SEO(sitemap/robots/메타), PWA(Serwist), 접근성, 분석 스니펫, 성능 점검, 도메인 연결·배포.

---

## 15. 환경변수 (이름만 — 값은 절대 커밋 금지)

| 변수 | 용도 |
|---|---|
| `KAKAO_JS_KEY` | 지도 SDK + 채널 채팅 버튼 |
| `KAKAO_REST_KEY` | 역지오코딩(좌표→구·동) |
| `KAKAO_CHANNEL_PUBLIC_ID` | 카카오톡 채널 1:1 채팅 대상 |
| (Cloudflare 바인딩) `DB` | D1 |
| (Cloudflare 바인딩) `BUCKET` | R2 |

> 운영 시크릿은 `wrangler secret put`, 로컬은 `.dev.vars`(gitignore). 자세한 규칙은 `CLAUDE.md` §환경변수.
> ※ 이전 버전의 `BREVO_*`(이메일) 변수는 카카오톡 채널 문의로 전환되며 **제거됨**.