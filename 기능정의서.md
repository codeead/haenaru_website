# 기능정의서 — 해나루 종합설비 홈페이지

> 이 문서는 Claude Code가 그대로 구현할 수 있도록 쓴 빌드 스펙이다. 사람이 읽는 제안서가 아니다.
> 구조·경로·가드레일은 `CLAUDE.md`를 함께 따른다.

---

## 0. 한 줄 요약

충남 당진 지역에서 배관·보일러·난방 설비를 시공하는 1인/소규모 업체(해나루 종합설비)의 홍보 + 시공사례(수리 이력) 홈페이지. **완전 정적 사이트**로 GitHub Pages(+커스텀 도메인)에 배포한다. 서버·DB가 없고 **관리자 화면(CMS)도 없다** — 수리 이력과 사진, 연락처 정보는 개발자가 레포에 직접 커밋해 관리한다. 문의는 전화(`tel:`)와 카카오톡 채널 링크로만 받는다 — 웹 폼/DB/이메일 없음. 개인정보처리방침 페이지 없음.

---

## 1. 프로젝트 개요

- **목적**: 신규 고객에게 신뢰를 주는 홍보 + 과거 시공 사례로 실력을 보여주는 포트폴리오.
- **사용자**:
  - 방문자(공개): 회사소개·서비스·수리 이력을 보고 전화/카카오톡으로 문의.
  - 관리자(개발자): 레포에 시공사례 폴더를 추가하고 push해서 반영 (`docs/시공사례-올리는법.md`).
    - 대표님이 현장 사진을 전달하면 개발자가 대신 올리는 흐름. 관리자 로그인 화면은 없다.
- **범위 밖(Non-goals)**:
  - 웹 문의 폼, 문의 DB, 이메일 발송
  - 회원가입/로그인, 관리자 CMS 화면, 결제
  - 실시간 트래픽 대시보드, 방문자 분석 도구(현재는 미도입 — 필요해지면 별도 논의)
  - 서버/백엔드/데이터베이스 (완전 정적 유지)
  - 개인정보처리방침 페이지(폼/추적이 없어 불필요)

---

## 2. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Astro (정적 출력 `output: "static"`) |
| 스타일 | Tailwind v4 (`@tailwindcss/vite`) |
| 콘텐츠 | Astro Content Collections (zod 스키마, `src/content.config.ts`) |
| 이미지 | `astro:assets` 내장 최적화(`sharp`) — 업로드 원본을 빌드 시 자동 리사이즈/WebP 변환 |
| SEO | `@astrojs/sitemap`, `robots.txt` |
| CMS | 없음 — 콘텐츠는 레포에 직접 커밋 |
| 호스팅 | GitHub Pages + GitHub Actions (`.github/workflows/deploy.yml`) |
| 지도/문의 연동 | Kakao Map 웹 링크(`map.kakao.com/link/...`, API 키 불필요), 카카오톡 채널 링크(`site/config.json` 값) |

UI 프레임워크(React 등)는 쓰지 않는다. 상호작용이 필요한 곳만 Astro `<script>` 바닐라 JS.

---

## 3. 사이트 구조 (IA)

```
/                   홈 (히어로 + 최근 수리 이력)
/about              회사소개
/services           서비스(취급 업무)
/works              수리 이력 목록
/works/[id]         수리 이력 상세
/location           오시는 길 (주소 + 지도 링크)
/contact            문의 (전화 + 카카오톡 채널)
```

공통: 헤더(로고+내비+전화 버튼), 푸터(연락처/등록업체 정보). 개인정보처리방침 페이지 없음.

---

## 4. 페이지별 스펙

### 4.1 홈 (`/`)
- **Hero**: 고정 팔레트 기반 배경(사진 캐러셀은 제거됨), 슬로건, "견적 문의하기" CTA(카카오/전화 버튼).
- **ServicesSummary / ContactCta**: 취급 업무 요약과 문의 유도 블록.
- **RepairHistory**: 최근 수리 이력 3건 카드 + "전체보기" 링크. 등록된 이력이 없으면 안내 문구 표시.

### 4.2 회사소개 (`/about`)
- 회사 소개 문단(`src/content/about.ts`), 대표자·등록업체·주소 요약 카드.

### 4.3 서비스 (`/services`)
- `src/content/services.ts`의 카테고리를 카드 그리드로 표시. (현재 8종: 보일러 시공 및 A/S, 난방 배관공사, 상하수도 배관공사, 누수 탐지, 해빙, 주택 리모델링, 조립식 건축, 코어작업)

### 4.4 수리 이력 목록/상세 (`/works`, `/works/[id]`)
- 목록: 발행된(`draft: false`) 항목을 최신순 카드 그리드로.
- 상세: 대표 사진, 카테고리, 지역, 날짜, 설명(마크다운 본문), 추가 사진 갤러리.
- 데이터 출처: `src/content/works/<slug>/index.md` (Astro Content Collections).

### 4.5 오시는 길 (`/location`)
- 전체 주소 텍스트(공개 확정됨), 영업시간, 전화번호.
- "카카오맵에서 보기" / "길찾기" 버튼 — Kakao Maps 웹 링크(`map.kakao.com/link/search`, `/link/map`)로 연결. API 키·SDK 불필요.

### 4.6 문의 (`/contact`)
- 전화 걸기 버튼 + 카카오톡 채널 버튼. 웹 폼 없음.
- 카카오톡 채널 링크가 아직 없으면(`kakaoChannelUrl` 빈 값) 카카오 버튼 자리에 전화 문의 버튼이 대신 표시됨 — 링크가 생기면 `site/config.json`에 붙여넣기만 하면 전 페이지에 자동 반영.

### 4.7 콘텐츠 관리 (관리자 화면 없음)
- 관리자 로그인 화면을 두지 않는다. 수리 이력은 `src/content/works/<slug>/index.md` + 같은 폴더 사진을 커밋해서 추가하고, 회사 정보는 `src/content/site/config.json`을 고친다.
- push하면 GitHub Actions가 빌드·배포한다. 규격과 절차는 `docs/시공사례-올리는법.md` 참고.
- 판단 근거: CMS 로그인에 필요한 Netlify Git Gateway가 deprecated 되었고, 사례 등록 빈도가 낮아 인증 인프라를 유지할 실익이 없었다. 콘텐츠 구조는 그대로 두었으므로 나중에 `public/admin/`만 되살리면 CMS를 다시 붙일 수 있다.

---

## 5. 데이터 모델 (Astro Content Collections)

`src/content.config.ts`의 `works` 컬렉션 (glob loader, `src/content/works/**/index.md`):

```
title            string, 필수
category         string, services.ts의 key와 일치
workedAt         date, 필수
locationRegion   string, 필수 — 동/읍 수준까지만 (정확한 지번/좌표는 수집하지 않음)
coverImage       image(), 필수 — 같은 폴더 상대경로
photos           { src: image(), alt: string }[], 기본 []
draft            boolean, 기본 false
본문(markdown)    선택 — 작업 설명
```

정확한 좌표(lat/lng)나 상세 지번은 애초에 수집하지 않는다 — 백엔드가 없어 자동 지오코딩이 불가능하고, 개별 시공 건 단위로는 공개 대상도 아니었다(회사 자체 주소는 `site/config.json`에서 별도로 전체 공개하기로 확정됨 — §7 참고).

`site/config.json`: 회사명, 대표자, tagline, 창업연도, 누적 시공 건수 보정값, 전화/휴대폰/팩스/이메일, 주소, 영업시간, 사업자등록번호, 등록업체 배열, 카카오톡 채널 링크.

---

## 6. 사진 처리

- **자동 EXIF/HEIC/리사이즈 파이프라인 없음** (백엔드가 없어 서버 처리가 불가능).
- 대신 Astro의 `astro:assets`가 **빌드 시점**에 자동으로 리사이즈/WebP 변환한다 (예: 480KB 원본 → 30~140KB WebP 여러 사이즈).
- 아이폰 사진은 HEIC라 처리되지 않는다. 카메라를 "높은 호환성(JPEG)" 형식으로 찍으면 변환이 필요 없다. 이 안내는 `docs/시공사례-올리는법.md`에 포함.
- 사진 저장 위치: 수리 이력 사진은 항목별 폴더(`src/content/works/<slug>/`)에 함께 저장한다.

---

## 7. 문의 및 위치 노출 정책

- 문의 채널은 **전화(`tel:`) + 카카오톡 채널 링크** 두 가지뿐. 웹 폼/DB/이메일 발송 없음.
- 카카오톡 채널 링크는 SDK 연동이 아니라 **`site/config.json`의 URL을 그대로 여는 `<a>` 링크**다. API 키가 필요 없고, 값이 없으면 전화 버튼으로 자동 대체된다.
- **주소 공개 범위**: 회사 주소(충남 당진시 아미로 828(대덕동))는 전체 공개하기로 사용자가 확정함 — `/location`, 푸터에 전체 주소 노출. (단, 개별 수리 이력 건의 위치는 여전히 동/읍 수준까지만 — §5.)

---

## 8. 누적 시공 건수

```
표시값 = baselineProjectCount(사이트 설정) + 발행된 works 항목 수
```
DB 카운트 쿼리 대신 Content Collections 길이를 빌드 시 계산한다.

---

## 9. 인증/보안

- 사이트 전체에 인증이 없다(완전 정적, 로그인 화면 자체가 없음). 콘텐츠를 고칠 수 있는 사람 = GitHub 레포 write 권한이 있는 사람.
- 따라서 공격 표면이 GitHub 계정 하나로 줄어든다 — 레포 계정에 2FA를 켜둔다.
- 민감정보(계좌번호 등)는 어떤 파일에도 커밋하지 않는다 — `명함.jpeg`는 `.gitignore` 처리됨.

---

## 10. 비기능 요구사항

- **SEO**: `@astrojs/sitemap`으로 sitemap.xml 자동 생성, `robots.txt`, 페이지별 title/description/OG 메타.
- **성능**: 이미지 자동 최적화, 정적 HTML이라 서버 응답 지연 없음.
- **접근성**: 시맨틱 마크업, 충분한 명도 대비, 포커스 가능한 인터랙션 요소에 적절한 label.
- **반응형**: 모바일 우선.
- **다크모드**: 쓰지 않는다 — 파스텔 팔레트가 어두운 바탕에서 탁해지고, 주 방문자에게 밝은 화면이 더 편하다.

---

## 11. 분석/모니터링

현재 도입하지 않음(방문자 분석 도구 없음 → 개인정보처리방침도 불필요). 필요해지면 별도로 논의 후 결정한다(예: 쿠키리스 분석 도구 도입 시 최소한의 고지 문구 추가 검토).

---

## 12. 디자인 토큰

`src/styles/tokens.css`에 CSS 변수로 정의, Tailwind `@theme inline`으로 유틸리티 클래스에 매핑. 적용 키트는 **"물과 빛"**(파스텔 바다 계열, 명도 층 4단계) — 팔레트 표와 선정 근거는 `.design/hanaru-brand/DESIGN_BRIEF.md`. 제목 서체는 고운돋움(제목 전용), 본문은 Pretendard. 컴포넌트는 hex를 직접 쓰지 않고 토큰/유틸리티만 사용한다.

---

## 13. 진행 순서 (완료 상태 표시)

1. ✅ Astro 스캐폴딩 + Tailwind + 실제 로고 기반 디자인 토큰
2. ✅ Content Collections(`works`) + `site/config.json` + `services.ts` — 명함 기준 실제 정보 반영
3. ✅ 공개 페이지 전부 구현 (홈/회사소개/서비스/수리이력/오시는길/문의)
4. ✅ 로고/이미지 자산 정리 + 브랜드 톤 적용
5. ✅ GitHub Actions 배포 워크플로우 작성
6. ✅ 도메인(`haenarueng.com`) 확정 및 관련 설정 반영 / CMS 제거
7. ⬜ **GitHub Pages 활성화, Spaceship DNS 레코드 교체, 커스텀 도메인 연결** — `docs/배포-설정-가이드.md`대로 웹 콘솔에서 직접 진행해야 하는 단계.
8. ⬜ 실제 수리 이력 콘텐츠 등록 (현재는 0건).
9. ⬜ 사업자등록번호·창업연도·누적 시공 건수 보정값 등 남은 플레이스홀더 값 확정.

---

## 14. 참고 문서

- 시공사례 추가 방법: `docs/시공사례-올리는법.md`
- 배포 및 최초 설정: `docs/배포-설정-가이드.md`
